# Recall-Climb Dead-Ends Log — every negative is a search-space cut

> Live log of what did NOT work climbing GSSM holographic recall past the ~9% wall,
> and WHY. Each negative narrows the search and leaves a breadcrumb. Nothing lost.
> (David's rule: negatives are data, not setbacks.)

## The standing facts these all branch from
- **Wall broken (headline):** key-conditioned holographic write = 8.89% ± 1.86% (5 seeds) vs 1.67% Selective floor. Verified.
- **The wall is CROSSTALK, not capacity:** recall vs n_pairs — 7.6%@8, 8.3%@4, **25.8%@2**. Fewer superposed pairs → much higher recall. The 25.8% at n=2 is the CEILING at n=8 if crosstalk is removed.
- **Readout crossover (disciplines EVERY cheap experiment):** `tanh_m` carries the effect at 1500 steps (~7%); `rms` collapses to ~2% under-trained but reaches 8.9% at 2500 steps. Any 1500-step sweep MUST use tanh_m or it measures readout-noise.

## Dead levers (measured negative)
| # | Lever | Result | WHY it failed (the breadcrumb) |
|---|---|---|---|
| 1 | **Additive phase** (Θ=cumsum ω) | 0.00pp | phase rotates blindly with TIME, not key identity → all values in one shared rotating bin, not separable. → led to: key-conditioned write (the WIN). |
| 2 | More channels (d_head 32→96) | flat | not width-limited; capacity is fine, interference is the enemy. |
| 3 | More heads (4→8) | −4.2pp | dilutes the holographic signal across heads. |
| 4 | More layers (2→3) | −4.9pp | extra mixing washes out the coherent read. |
| 5 | Separate write/read key (Q≠K) | −3.65pp | shared key GUARANTEES cos(φ_k−φ_q)=1 for the matched pair by construction; separating breaks that self-consistency. |
| 6 | phase_scale widening (2π, 3π) | −2pp, −3.9pp | "spread keys on the circle" attacks a NON-dominant term (collisions); the real limit is the read random-walk. Spreading backfires. |
| 7 | **Z3 multi-PHASE READ** (n offset de-rotations) | n=3 (2.6%) < n=1 (4.1%) | the across-j read is RANK-2 (re,im) = exactly the n=1 read. coh_energy ≡ 2·pedestal. P_tot−P_coh carries crosstalk ENERGY (not info-free) but it's DETECTION ≠ RECONSTRUCTION — you see the interference, can't recover the drowned value. Read-side separation is the wrong axis. |
| 8 | Multi-FREQ write run #1 (rms readout, 1500 steps) | K=1 itself = 1.48% (chance) | CONFOUND, not a real result: rms collapses under-trained (fact above). The K=1 baseline was dead, so K=2/4 numbers meaningless. Fix: tanh_m readout. Re-running. The MECHANISM is byte-correct (K=1==baseline max|Δ|=0, reduction exact). |
| 9 | **Multi-FREQ write** (k·φ harmonics, K bands, tanh_m, 1500 steps) | K=1 5.21%, **K=2 6.28% (PEAK, +1pp, std shrank 3.6×)**, K=4 3.73%, K=8 1.82% (chance) | A real but TINY gain at K=2 then monotone decay. Diagnosis (4 hypotheses tested): NOT seed-noise (K=8 clean), NOT Chebyshev-redundant (band-reads full rank 8/8, self-measured), NOT learning (c_k falls at INIT 0.98→0.74), NOT combine (learned weights = equal). The bands are INDEPENDENT but STRUCTURALLY UNEQUAL in matched-coherence from birth (c_k falls with k) — equal-weight summing drowns the strong band in weak ones. K=2 banked as a small real lever; harmonics-of-same-key is capped. |
| 10 | **Multi-SLOT partition** (M disjoint accumulators, learned router) | s1 8.25%/11.38% > ALL multi-slot; **corr(slot-entropy, recall) = −0.46** (self-measured) | Router collapsed first (entropy 23-26%); Gumbel+entropy-reg λ=0.03 fixed it at s8 (entropy 77%) — but the BETTER-partitioned the slots, the LOWER the recall. Partition is ANTI-correlated with recall. Splitting the bounded state destroys the shared coherent superposition + m-gate that makes the holographic write work. |
| 11 | **Ginibre / β=3 repulsive vector keys** (D-dim keys + matched-filter read + cubic repulsion loss, λ=0.03) — *2026-06-25 overnight* | vec_key (no rep) **3.03%±0.88% < 1D baseline 4.69%**; vec_key_rep (β=3) **4.26%±1.62% ≈ baseline**. Threshold ≥15% CONFIRM / ≤11% KILL → **KILL.** Reductions exact, attn 0.9976. | TWO diagnoses: (1) without repulsion the D-dim key phases **collapse to ~0** — the matched-filter gradient through W_key is too weak, task gradient does not drive phase spreading; the vector-key architecture is INERT without external repulsion. Dimensionality alone buys nothing. (2) λ=0.03 too weak to bite: **key-cloud ⟨s²⟩ stuck at 1.00–1.04, β=3 target 1.087 NEVER reached** — Arm C never entered the spread-key regime. We killed "this λ spreads keys", NOT yet "spread keys → higher recall". One open thread (push λ→0.1/0.3, 3000+ steps). |
| 12 | **Resonator / bounded iterative phase-cleanup** (K∈{0,1,2,3} Newton steps on Im residual) — *2026-06-25 overnight* | K=0 base **7.23%±1.78%**; **K≥1 CRASHES to floor**: K1=1.40%, K2=1.38%, K3=1.71% (≈ holo_off 1.48%). Adversary fresh seeds {13,99}: K1/K2/K3 all ~1.4–1.6% confirmed. Both reduction gates exact, attn 1.0000. **KILL.** | The cleanup DESTROYS the binding, not just fails to help. THREE diagnoses (all reproduced by adversary on independent seeds): (1) Im(S·e^{−iφ}) with N=8 superposed is itself full of crosstalk → the step pushes the query AWAY from the matched phase. Resonator needs LOW crosstalk to work but was built to REDUCE it — **self-defeating by construction.** (2) λ step-size **untrainable** — soft-sign grad saturates to 0 except when |im|≈0; λ wandered aimlessly in (−0.073,+0.025), no systematic sign. (3) write & read angles share W_key → perturbing the read angle breaks the trained write-read alignment; crash to floor = holographic path removed. |

## META-FINDING (updated 2026-06-25 — now FIVE non-wins, converging)
**TWO MORE qualitatively-different levers down, and they converge with the first three.** The earlier three "force separation in the bounded state" tricks (multi-phase read, multi-freq write, multi-slot) all failed because partitioning destroys the shared coherent superposition gated by m. Tonight's two add the sharper structural lesson: **two canonical VSA above-capacity tools — determinantal/Ginibre key codes AND resonator cleanup — BOTH require low crosstalk to function, and therefore neither can be the tool that fixes high crosstalk.** Ginibre keys are inert without repulsion (and our repulsion never reached the spread regime); the resonator's cleanup gradient is itself crosstalk-poisoned and self-defeating. Five qualitatively different levers, five non-wins, all triangulating the SAME limit. **The 8.89% is looking like a genuine single-bounded-channel holographic capacity limit at n_pairs=8, not an engineering artifact.** ONE cheap experiment still open (Ginibre λ↑ to actually reach ⟨s²⟩=1.087) before that reading is decisive. After that, the honest next axis is NOT another in-channel separation trick — it is the architectural bypass (GSSM state + 1 small attention head) or accepting + writing up the capacity limit.

## ORIGINAL META-FINDING (the prior day's structural lesson, preserved)
**All THREE "separate the crosstalk spatially/spectrally" axes FAILED or capped:** multi-phase read (rank-2 dead), multi-freq write (K=2 +1pp cap), multi-slot partition (anti-correlated). The crosstalk DIAGNOSIS is real (n_pairs=2 → 25.8%), but every attempt to FORCE separation in the bounded state fails — because partitioning destroys the one thing that makes the holographic write work: the shared coherent superposition gated by m. **The 8.89% may be near the limit of a SINGLE bounded-state channel's holographic recall at n_pairs=8.** The next lever must be QUALITATIVELY different — not another separation trick. Candidates: (a) Ginibre/β=3 repulsive KEY GEOMETRY (give keys a 2D complex-plane geometry where they don't overlap by construction — One_Constant corpus), (b) resonator / iterative cleanup (the real VSA >capacity tool, deferred earlier as an attention-loop on the read).

## What the chain narrows the search to (updated 2026-06-25)
- Capacity levers (more channels/heads/layers): **all dead.** Stop adding resources.
- Read-side separation: **dead** (rank-2 trapped). Stop fixing at read time.
- Write-side frequency multiplexing (multi-freq, K bands k·φ): **RESOLVED — capped at K=2 (+1pp).** Harmonics-of-same-key cannot carry it.
- Key geometry (Ginibre/β=3 determinantal): **dead as built** (inert without repulsion; our repulsion never reached the spread regime). ONE open thread: λ↑ to actually hit ⟨s²⟩=1.087 — close it cheaply, then it's done either way.
- Iterative cleanup (resonator): **dead — self-defeating** (cleanup gradient is crosstalk-poisoned; crashes the binding to floor).
- **The honest remaining axis is NO LONGER in-channel.** Next moves: (A) close the Ginibre λ thread (decisive cheap negative or surprise win); (B) architectural bypass — GSSM state + 1 SMALL attention head (tiny KV dim), O(T) but the honest way above the holographic ceiling; (C) n_layers 2→4 to compose multiple reads (cheaper, lower EV); (D) accept 8.89% as a single-bounded-channel capacity limit and write it up with the n_pairs crosstalk curve + 5 triangulating negatives.

## Process breadcrumbs (so we don't repeat)
- ALWAYS pin readout = tanh_m for ≤1500-step cheap sweeps (the rms confound bit us once).
- ALWAYS verify the K=1 / baseline arm reproduces ~9% before trusting the swept arms — a dead baseline = a confounded sweep.
- Multi-seed mandatory; single-seed positives on a chance-flat loss are noise (the seed-42 phase-GSSM false positive).
- Self-test reduction (use_phase=False == Selective, K=1 == baseline) catches mechanism bugs; a passing reduction + chance recall = a RUNNER/config bug, not a mechanism bug.
